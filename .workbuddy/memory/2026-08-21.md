# 2026-08-21 工作日志

## 书签整理项目（bookmarks_2026_8_21.html）

**源文件**: `/Users/yourname/Documents/bookmarks_2026_8_21.html`（0.8MB，797 个书签，Netscape 格式，含大量 base64 ICON 拖累体积）

**成果**:
- 生成整理后文件 `bookmarks_整理后.html`（113.5 KB，781 个有效书签，标签配对完整：46 DL / 45 H3，无空文件夹）
- 清理 4 个垃圾条目（新标签页、chrome-native://newtab、go.itab.link、过期电视列表）+ 站点级合并 12 个重复
- 10 个顶层文件夹：AI 工具(92)、电子硬件(290)、金融与投资(65)、NAS 与服务器(30)、开发与编程(61)、学习资源(3)、日常娱乐(112)、工具与实用(43)、生活与工作(65)、私密(22)
- 脚本 `classify_bookmarks.py` v2 可复现：关键词词边界匹配（kw）+ 子串匹配（has）+ 站点合并 + 兜底修正

**关键经验（可复用）**:
1. 大文件解析用 Python 而非 Read；栈结构解析嵌套 DL/H3 层级
2. URL 规范化去重：去 utm/spm 跟踪参数、www、协议、尾部斜杠，小写后精确去重
3. 分类优先级：私密(成人) → 电子硬件 → 金融/AI/NAS → 日常娱乐 → 开发 → 学习 → 工具 → 生活 → 兜底
4. 子串匹配陷阱：`nas` 误配 NASA、`git` 误配 react-tetris → 必须用词边界正则 `(?<![a-z0-9])word(?![a-z0-9])`
5. 兜底分支会吞掉工具类条目（"日常"路径 → 日常杂项里混入 14 个工具）→ 需在兜底前加"回捞修正"规则
6. 删除 base64 ICON 数据可大幅压缩体积（0.8MB → 113KB）

## 个人网址导航站（My Nav）

**产物**: `index.html`（396 KB，单文件，781 个站点，22 个快捷入口）
**生成脚本**: `gen_nav.py`（exec 截断方式复用 classify_bookmarks.py 的解析+分类逻辑）

**导航站特性**:
- 苹果简约风（浅色 #f5f5f7 底 + 白卡片 + 圆角 + 柔和阴影，`-apple-system/PingFang` 字体）
- 实时搜索（⌘K 聚焦 / Esc 清空，匹配 title/url/域名，自动隐藏空分组）
- 分类可折叠；私密分类默认折叠+模糊锁定，点击解锁
- 深色/浅色主题切换（localStorage 记忆 + 跟随系统）
- 分类主题色：AI 靛蓝 / 硬件青绿 / 金融玫红 / NAS 紫 / 开发蓝 / 学习绿 / 娱乐橙 / 工具青 / 生活蓝紫 / 私密灰
- 每个链接卡片用「分类色渐变 + 首字母」圆标，零外部依赖、离线可用

**关键经验**:
1. Python f-string 与 JS 模板字符串花括号冲突 → script 块用普通字符串 + `__QUICK_JSON__` 占位符 replace
2. exec(compile(src[:cut])) 截断复用已有脚本逻辑，避免 import 副作用
3. 快捷入口必须从书签真实数据中查找（URL 子串匹配），保证不出现书签里没有的站点
4. color-mix() + backdrop-filter 实现毛玻璃顶栏；CSS 变量实现主题切换

## 导航站升级：品牌色 + favicon（2026-08-24）

- 每张卡片注入 `style="--brand:颜色"`，hover 边框/阴影用 color-mix(var(--brand)) 替代统一强调色
- 图标改为网站 logo：`favicon.im/{domain}?larger=true`，onerror 二级回退 duckduckgo icons，再失败隐藏露出品牌色字母圆标（内网 IP/localhost 不加载）
- 品牌色映射：BRAND 字典约 70 个常见域名（claude #d97757、github #24292f、bilibili #fb7299…），逐级去子域匹配（blog.csdn.net → csdn.net）；未收录域名用 md5 哈希生成稳定 HSL 色
- 快捷入口 JSON 扩展第 4 元素为品牌色
- 产物：`index.html`（566.9 KB，763 个 favicon 引用，781 个品牌色注入）

## 导航站升级：分类侧边栏（2026-08-24 第二轮）

- 桌面端左侧固定侧边栏（sticky top:78px，196px 宽，隐藏滚动条）：分类色点 + 名称 + 数量，点击平滑滚动定位（getBoundingClientRect 计算并减 70px 顶栏偏移）
- IntersectionObserver（rootMargin '-25% 0px -65% 0px'）实现滚动时当前分类高亮，侧边栏当前项自动 scrollIntoView 保持可视
- 窄屏（≤1100px）侧边栏隐藏，替换为顶部 sticky 横滑标签条（chips），毛玻璃底
- 搜索过滤时联动隐藏侧边栏/标签条对应分类项（navMap 映射）
- 布局重构：hero/quick max-width 1400 对齐 .layout（flex：sidebar 196 + gap 34 + main flex:1）；main 去掉独立 max-width
- 二级子分类：侧边栏分类行带 chevron，点击行 = 手风琴展开子列表 + 滚动到分类；子项点击滚到对应 .sub 区块；滚动时 ioSub 观察器高亮当前子分类、io 观察器自动展开当前分类并收起其他（手风琴）；私密分类不展示子列表；navMap 改为 [group, chip]，搜索隐藏整组
